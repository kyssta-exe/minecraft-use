# minecraft-use Tests

## Running Tests

```bash
npm test
```

## Test Structure

- `tests/skills.test.js` — Skill library CRUD and search
- `tests/confidence.test.js` — Confidence engine patterns
- `tests/context.test.js` — Context builder token estimation
- `tests/memory.test.js` — Memory system operations
